---
// src/components/Texto.astro
---
<div class="text-animation">
    {Array.from("Hola, soy Javi").map((char, index) => (
      <span style={`animation-delay: ${index * 0.1}s;`}>{char}</span>
    ))}
  </div>
  
  <style>
  .text-animation {
    font-size: 2rem;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
  }
  
  .text-animation span {
    opacity: 0;
    animation: fadeIn 0.5s forwards;
  }
  
  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }
  </style>
  